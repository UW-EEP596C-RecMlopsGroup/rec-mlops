name: CI/CD Pipeline

# Trigger conditions:
# - Run the CI pipeline when a pull request targets main
# - Run the full CI + CD pipeline when code is merged into main
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  ci:
    name: Run Tests & Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8
      - name: Run unit tests
        run: pytest tests/unit
      - name: Run flake8 lint
        run: flake8 src/ tests/

  cd:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: ci
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.api
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/rec-mlops:latest
      # Optional: trigger a Prefect Flow run
      # - name: Trigger Prefect Flow
      #   run: |
      #     curl -X POST <PREFECT_API_ENDPOINT> -H "Authorization: Bearer ${{ secrets.PREFECT_TOKEN }}"
      # Automatically update the k8s deployment (requires a custom script)
      # - name: Update k8s deployment
      #   run: |
      #     python scripts/update_k8s_deployment.py --image ${{ secrets.DOCKERHUB_USERNAME }}/rec-mlops:latest
      #     kubectl apply -f k8s/
